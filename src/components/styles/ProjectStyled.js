import styled from '@emotion/styled';

const ProjectStyled = styled.div`
  overflow: hidden;

  a {
    display: grid;
  }

  .banner {
    width: 550px;
    height: 300px;
  }

  img {
    top: 0;
    bottom: 0;
    left: 0;
    right: 0;
    margin: auto;
    padding: 25px;
    object-fit: contain;
  }
`;

export default ProjectStyled;
